<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.marcelomonier.descartaqui.descartaqui">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MenuActivity">

        </activity>
        <activity android:name=".ApresentacaoActivity" />
        <activity android:name=".LocaisActivity" />
        <activity android:name=".ReeesActivity" />
        <activity android:name=".RiscosActivity" />
        <activity android:name=".AcondicionamentoActivity" />
        <activity android:name=".Menu2Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>